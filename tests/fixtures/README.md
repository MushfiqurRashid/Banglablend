# Test fixtures

Production-like integration fixtures are created by `pnpm seed` and are explicitly marked `isPlaceholder: true` and `verified: false`. Keep credentials in test environment variables. Do not commit customer data or payment credentials here.
